dependencies:
  pre:
    - npm install -g npm dependency-check
deployment:
  master:
    branch: master
    commands:
      - npm run build && aws s3 sync dist s3://xebia-vis-ma-vie--master/ --delete
    region: eu-west-1
