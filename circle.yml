dependencies:
  pre:
    - npm install -g npm dependency-check
deployment:
  master:
    branch: master
    commands:
      - npm run build && npm run robots-txt && aws s3api put-bucket-policy --bucket xebia-vis-ma-vie--master --policy file://aws/bucket-policy.preprod.json --region eu-central-1 && aws s3 sync dist s3://xebia-vis-ma-vie--master/ --delete --region eu-central-1
machine:
  node:
    version: 6.9.1
