import Vue from 'vue'
import App from 'src/main/components/App'

describe('App.vue', () => {

	it('should render correct contents', () => {

	 //    const vm = new Vue({
		// 	el: document.createElement('div'),
		// 	template: "<App title='My title' />",
		// 	components: {
		// 		App: App
		// 	}
	 //    });

		// expect(vm.$el.querySelector('.App-title').textContent)
	 //    .to.equal('My title world !!!')
	})
})
